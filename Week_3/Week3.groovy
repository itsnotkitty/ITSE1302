Map<String,List<Integer>> studentGrades =[
    'Jim':[76,92,60,85,49,99]
    
]

studentGrade.each { k,v ->
    BigDecimal average = v.sum() / v.size()
    if (average >= 90 && <= 100 {
        letterGrade = 'a'
    } else if (average < 60
}
